# English Expanded Final — CyberStep 1.509.0

The final package restores the user's preferred O/o/x member status markers, with the original English package spacing. CompactResource verifies these three existing values and leaves dic.conf byte-for-byte unchanged. Only the two existing compact labels in kwtdic.conf are changed relative to the pinned English Final resource baseline.

MC titles and conditions remain original. Existing English item, quest and story display translations remain enabled. NPC names, images and cache are outside the translation scope. Final denotes the selected release, not complete translation of every game text.

Install the three KAR files from the package into programs/ and resources/ while the game and updater are closed, preserving original backups. Launch normally; no BAT is required. This package targets CyberStep 1.509.0 only.

The build verifies catalog mappings, class loading, system-effect cases, all archive members and ZIP contents. Visual testing of this Final package remains pending.
